export interface Patient {
  id: string;
  name: string;
  birthdate: string;
  email: string;
  address: string;
}

export interface SidebarType {
  title: string;
  path: string;
  className: string;
}